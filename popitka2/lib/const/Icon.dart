import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';
import 'package:popitka2/const/Color.dart';

class appIcon {
  static const Icon iconAdd = Icon(Icons.add, color: appColors.textColor);
  static const Icon cancel = Icon(
    Icons.cancel,
    color: appColors.textColor,
  );
  static const Icon arrowDown = Icon(Icons.arrow_downward);
}
